<div>
    <%= link_to team.name, team_path(team.id), class: '' %>
    <%= link_to '編集', edit_team_path(team.id), class: '' %>
    <%= link_to '削除', team, method: :delete, data: { confirm: "#{team.name}を削除しますか?" } %>
</div>